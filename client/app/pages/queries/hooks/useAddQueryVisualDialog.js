import React from 'react';
import { Modal } from 'antd';

export default function useAddQueryVisualDialog() {
    return (
    <Modal
        title="Modal"
        okText="确认"
        cancelText="取消">
    </Modal>
    );
}
